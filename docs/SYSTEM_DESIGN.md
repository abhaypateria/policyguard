# System Design — explained simply

> Plain-language explanations of every concept, written so I can defend them in an
> interview. Filled in as we build.

Topics to cover (added as we reach them):

- What an **embedding** is (meaning turned into numbers)
- What a **vector database** is and why **pgvector** instead of a normal table
- **Chunking** strategy and chunk-size trade-offs
- **top-K** and the **similarity threshold**
- **RAG vs. fine-tuning**
- **Hallucination** and **grounding**
- How the **self-correction loop** works
- Why an **audit log** matters to a regulator
- How **versioning** works
- How I'd **scale it** (hybrid search + reranking, caching, async ingestion,
  multi-tenancy/permissions, observability)

_Starts filling in on Day 1._
